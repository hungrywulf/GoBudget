package main

/* Generate items
for i := 1; i <= 2153; i++ {
	loops := rand.Intn(5)
	userID := i
	for j := 0; j < loops; j++ {
		date := time.Now()
		itemAmount := math.Floor((rand.Float64()*2850+2)*100) / 100
		increasesR := rand.Intn(2)
		reoccuringR := rand.Intn(5)

		db.Create(&Items{User_ID: uint(userID), Date: date, Amount: itemAmount, Increases: increasesM[increasesR], Reoccuring: reoccuringM[reoccuringR]})
	}
}
*/

// Generate invested_stock

// Generate budget

// Populate Stock

// Generate bond
